---
title: "PAM"
author: "LaurenZane"
date: "8/2/2022"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}

library(ggplot2)
dark.adaptation <- read.csv("/Users/laurenzane/Desktop/Putnam_Lab/Projects/Coral_CNT/Data/20220802-dark-adaptation.csv")
sat.intensity <- read.csv("/Users/laurenzane/Desktop/Putnam_Lab/Projects/Coral_CNT/Data/2022-sat-intensity.csv")
```

```{r}

dark.adaptation.plot <- ggplot(dark.adaptation, aes(x=Time.point, y=Y)) + geom_line() + geom_point() + theme_classic()
print(dark.adaptation.plot)

sat.intensity.plot <- ggplot(sat.intensity, aes(x=sat.intensity, y=Y)) + geom_line()
print(sat.intensity.plot)
combo <- ggarrange(dark.adaptation.plot, sat.intensity, 
          ncol = 2, nrow = 2)
print(combo)



```
